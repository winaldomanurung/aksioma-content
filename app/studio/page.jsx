import StudioClient from "@/components/studio/StudioClient";

export const metadata={
  title:"Carousel Studio | Aksioma",
  description:"Paste JSON carousel, preview, and export JPEG in the browser."
};

export default function StudioPage(){
  return <StudioClient/>;
}
